import React, { useState, useEffect } from "react";

 const [count, setCount] = useState(1);

export default  function CardInfo({Titulo, data, horarioInicio, horarioFim, Palestrante, Descricao, Foto}){

    return(<div> </div>);

}